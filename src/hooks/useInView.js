import { useEffect } from 'react'

// Placeholder hook - we use framer-motion's whileInView in MotionWrap for simplicity
export default function useInView() {
  useEffect(() => {
    // no-op: kept for future expansion
  }, [])
}
